apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecommerce-client.fullname" . }}
  labels:
    {{- include "ecommerce-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecommerce-client.selectorLabels" . | nindent 4 }}
